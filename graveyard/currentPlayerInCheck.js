// is the current player in check?

// we need a psuedo-state "highlightedSquares" for these functions? IDTS

export function currentPlayerInCheck(currentPlayer, board){
  // for every enemy piece on the board...
      // calculate its available squares,
      // if the piece's capture squares have current colors king in them then:
      //return true
}
